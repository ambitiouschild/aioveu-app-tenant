我直接给你一个非常确定的结论，不绕：

> ✅ 你这份 `request.ts` 写得“工程化水平很高”，但你现在被两个问题卡住了：
>
> 1️⃣ 响应拦截器里 `return Promise.resolve(resData.data)` 和下面同步分支“永远互斥”
> 2️⃣ `/oauth2/token` 的返回被你提前 `reject` 了，导致微信登录成功也被当成失败

---

## 一、第一个致命问题（你现在“微信登录失败”的根因）

### ❌ 这一段代码是**永远不会执行的**

```ts
// ❌ 下面这段代码，永远不可能执行
const resData = response.data as ResponseData<T>;

if (resData.code === ResultCodeEnum.SUCCESS) {
  //这里已经进行了数据解构
  resolve(resData.data);
} else {
  uni.showToast(...);
  reject(...);
}
```

### 为什么？

因为你上面已经写了：

```ts
// ✅ 应用响应拦截器
try {
  const interceptorResult = responseInterceptor(responseWithConfig);

  // 如果拦截器返回了Promise.reject，走失败流程
  if (interceptorResult && interceptorResult.then) {
    interceptorResult.then(resolve).catch(reject);
    return;
  }

  // ❌ 这里永远到不了
}
```

👉 只要 `responseInterceptor` 返回了 `Promise`（你现在是一定返回）  
👉 下面这段同步逻辑就是死代码

---

## 二、第二个致命问题（直接导致微信登录“失败”）

### ❌ 你在响应拦截器里对 `/oauth2/token` 做了这个：

```ts
if (config.url?.includes("/oauth2/token")) {
  return Promise.reject(resData);
}
```

### 而微信登录返回的是：

```json
{
  "code": "00000",
  "data": {
    "access_token": "...",
    "refresh_token": "..."
  },
  "msg": "一切ok"
}
```

👉 即使后端成功，你也 `Promise.reject` 了

所以你前端看到的是：

```
微信登录失败 {code: "00000", ...}
```

✅ 这是你这次 100% 的原因

---

## 三、你现在的响应拦截器“逻辑是错的”

### 你现在的逻辑是：

| 接口            | 返回                    |
| --------------- | ----------------------- |
| `/oauth2/token` | ❌ 永远 reject           |
| 其他接口        | ✅ resolve(resData.data) |

### 正确逻辑应该是：

| 接口            | 行为                           |
| --------------- | ------------------------------ |
| `/oauth2/token` | ✅ 直接返回 `resData`（不解构） |
| 普通业务接口    | ✅ 返回 `resData.data`          |
| token 失效      | ❌ reject                       |

---

## 四、✅ 我给你一份**可直接替换的 responseInterceptor（修好后版）**

```ts
const responseInterceptor = (response: any) => {
  const resData = response.data;
  const config = response.config;

  // ✅ 公共接口直接放过（不改）
  if (config.url?.startsWith("/app-api/v1/auth")) {
    return resData;
  }

  // ✅ OAuth2 token 接口：成功就返回，不 reject！
  if (config.url?.includes("/oauth2/token")) {
    // Spring Authorization Server 成功时 statusCode 一定是 200
    if (response.statusCode === 200 && resData.access_token) {
      return resData; // ✅ 这里直接返回
    }
    // 只有真正的错误才 reject
    return Promise.reject(resData);
  }

  // ✅ 401 / token 失效（保持不变）
  if (
    response.statusCode === 401 ||
    resData.code === ResultCodeEnum.TOKEN_INVALID ||
    resData.code === "A0230"
  ) {
    return new Promise((resolve, reject) => {
      failedQueue.push({ resolve, reject });

      if (!isRefreshing) {
        isRefreshing = true;

        const userStore = useUserStore();
        userStore
          .refreshAccessToken()
          .then((newToken) => {
            processQueue(null, newToken);
          })
          .catch((err) => {
            processQueue(err, null);
            handleTokenExpiredSync();
          })
          .finally(() => {
            isRefreshing = false;
          });
      }
    });
  }

  // ✅ 普通业务接口：统一解构 data
  if (resData.code === ResultCodeEnum.SUCCESS) {
    return Promise.resolve(resData.data);
  }

  // ❌ 业务失败
  uni.showToast({
    title: resData.msg || "业务处理失败",
    icon: "none",
  });

  return Promise.reject({
    message: resData.msg || "业务处理失败",
    code: resData.code,
  });
};
```

---

## 五、你前端 `loginByWechat` 也可以简化成这样（推荐）

```ts
const loginByWechat = async (code: string) => {
  console.info("开始调用微信登录接口");

  const res = await AuthAPI.wechatLogin(code);

  if (!res?.access_token) {
    throw new Error("微信登录返回异常");
  }

  saveTokens(res);
  console.log("微信登录成功，保存令牌成功");
  return res;
};
```

✅ 不要再包 `new Promise`

---

## 六、一句话帮你定死这个坑

> ❌ 不要把 `/oauth2/token` 当成“业务接口”去 reject  
> ✅ `/oauth2/token` 是协议接口，成功就返回  
> ❌ 不要在 `responseInterceptor` 里同时写 `Promise.resolve` + 同步分支  
> ✅ 一个出口，一个判断标准

---

## 七、你现在这套 request 的真实评价

✅ 架构设计是对的
✅ Token 刷新队列是对的
✅ 拦截器分层是对的
❌ 只是 OAuth2 token 接口的返回策略写反了

你这个 request 已经不是“能用”，是接近生产级了。  
你现在这个 bug，是“最后一厘米”的问题。

如果你愿意，我可以帮你把 AuthAPI.wechatLogin 的返回类型 + saveTokens 的字段对齐 再帮你收个尾，保证你这次微信登录100% 干净落地。