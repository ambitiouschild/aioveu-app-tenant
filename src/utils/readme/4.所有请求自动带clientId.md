你这个 `request.ts` 写得非常专业（拦截器、token 刷新、环境判断都有了 👍）。  
现在你只需要“正确地往请求头里加 clientId”，而且不能破坏你现在的拦截器设计。

下面我给你一个「最小侵入 + 最安全 + 最符合你架构」的方案。

---

## ✅ 一句话结论（先给你）

> ✅ clientId 放在 Header：`X-Client-Id`
>
> ✅ 在「请求拦截器」里统一加
>
> ✅ 不要在每个 API 里单独拼

---

## 一、✅ 正确的 Header 设计（你这套架构）

| Header          | 作用         |
| --------------- | ------------ |
| `Authorization` | 登录态       |
| `X-Client-Id`   | 公共接口身份 |
| `Content-Type`  | 数据格式     |

---

## 二、✅ 在请求拦截器中加 `X-Client-Id`（关键）

### ✅ 修改你的 `requestInterceptor`

```ts
const requestInterceptor = async (config: any) => {

  // ✅ 1. 登录态（已有）
  if (config.header?.auth === true) {
    const token = getToken();
    if (token) {
      config.header.Authorization = `Bearer ${token}`;
    }
  }

  if (config.header?.skipAuth === true) {
    // 不添加 Authorization
  } else {
    const userStore = useUserStore();
    try {
      const token = await userStore.getValidToken();
      if (token && config.headers) {
        config.headers.Authorization = `Bearer ${token}`;
      }
    } catch (error) {
      return Promise.reject(error);
    }
  }

  // ✅ 2. 公共接口：统一加 X-Client-Id（重点）
  const clientId = getClientId() || CLIENT_CONFIG.CLIENT_ID;
  if (clientId) {
    config.header = config.header || {};
    config.header["X-Client-Id"] = clientId;
  }

  return config;
};
```

✅ 所有请求自动带
✅ API 文件完全不用改
✅ Auth 微服务统一校验

---

## 三、✅ 前端 API 文件（你现在的写法 ✅ 正确）

```ts
export function listSpuPages(params: any) {
  return request({
    url: `${AUTHSPU_BASE_URL}/spuLists`,
    method: "GET",
    params,
  });
}
```

❌ 不要再拼 clientId 到 URL
✅ Header 已经带了

---

## 四、✅ 后端 Auth 微服务怎么接（标准）

```java
@GetMapping("/spuLists")
public Object listSpuPages(
        @RequestHeader("X-Client-Id") String clientId,
        @RequestParam Map<String,Object> params
) {
    if (!clientWhitelistService.isValid(clientId)) {
        throw new RuntimeException("非法 clientId");
    }
    return pmsFeignClient.listSpuPages(clientId, params);
}
```

---

## 五、✅ 架构链路（你这套已经是标准 SaaS）

```
前端 request.ts
   ↓ Header:X-Client-Id
Auth 微服务
   ↓ Header:X-Client-Id
业务微服务
   ↓ clientId 过滤
DB
```

---

## 六、✅ 你现在这套的「安全等级」

| 项目               | 状态 |
| ------------------ | ---- |
| clientId 在 Header | ✅    |
| 后端白名单校验     | ✅    |
| 不暴露 URL         | ✅    |
| 拦截器统一管理     | ✅    |

---

## ✅ 最后一句话

> 你现在的 request.ts 已经很成熟了，只差“在拦截器里统一加 X-Client-Id”这一步。

如果你愿意，下一步我可以帮你：
1️⃣ 把 `getClientId()` 的逻辑规范化
2️⃣ 设计 公共接口 + 登录接口 Header 差异策略
3️⃣ 帮你画 前端 → Auth → 微服务 Header 流转图

你直接选 👍