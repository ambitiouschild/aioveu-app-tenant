好，这个文件已经非常接近“生产级订单详情页”了，而且你现在的问题是非常典型、也非常高级的“前后端枚举对齐问题”。

我直接给你一份“根据你后端 `OrderStatusEnum` 100% 对齐后的最终可用版本”，只改关键地方，不动你现有结构和样式。

---

# ✅ 一、先给你结论（重点）

> ✅ 你后端 `OrderStatusEnum` 是唯一真理源
>
> ❌ 前端不能再出现 `pending / paid / shipped / completed / refunded`
>
> ✅ 模板、JS、CSS 全部用 `OrderStatusEnum`
>
> ✅ 文案、样式、逻辑全部收口到枚举

---

# 二、你后端枚举（唯一真理）

```java
UNPAID(0, "待付款"),
PAID(1, "已付款(待发货)"),
SHIPPED(2, "已发货"),
COMPLETE(3, "已完成"),
CANCELED(4, "已取消"),
CLOSED(5, "已关闭"),
SERVICING(6, "售后中");
```

---

# 三、✅ 前端枚举（请你直接替换）

### `src/packageD/enums/OrderStatusEnum.ts`

```ts
/**
 * 订单状态枚举
 * ⚠️ 必须与后端 OrderStatusEnum 完全一致
 */
export enum OrderStatusEnum {
  /** 待付款 */
  UNPAID = 0,
  /** 已付款（待发货） */
  PAID = 1,
  /** 已发货 */
  SHIPPED = 2,
  /** 已完成 */
  COMPLETE = 3,
  /** 已取消 */
  CANCELED = 4,
  /** 已关闭 */
  CLOSED = 5,
  /** 售后中 */
  SERVICING = 6,
}

/**
 * 订单状态文案
 */
export const OrderStatusLabel: Record<OrderStatusEnum, string> = {
  [OrderStatusEnum.UNPAID]: "待付款",
  [OrderStatusEnum.PAID]: "已付款（待发货）",
  [OrderStatusEnum.SHIPPED]: "已发货",
  [OrderStatusEnum.COMPLETE]: "已完成",
  [OrderStatusEnum.CANCELED]: "已取消",
  [OrderStatusEnum.CLOSED]: "已关闭",
  [OrderStatusEnum.SERVICING]: "售后中",
};

/**
 * 订单状态 → CSS 类名
 */
export const OrderStatusClass: Record<OrderStatusEnum, string> = {
  [OrderStatusEnum.UNPAID]: "pending",
  [OrderStatusEnum.PAID]: "paid",
  [OrderStatusEnum.SHIPPED]: "shipped",
  [OrderStatusEnum.COMPLETE]: "completed",
  [OrderStatusEnum.CANCELED]: "cancelled",
  [OrderStatusEnum.CLOSED]: "closed",
  [OrderStatusEnum.SERVICING]: "servicing",
};
```

---

# 四、✅ Template 必须这样改（核心）

## 1️⃣ 状态栏（✅ 唯一正确写法）

```html
<view class="status-section" :class="OrderStatusClass[orderDetail.status]">
  <view class="status-icon">
    <text v-if="orderDetail.status === OrderStatusEnum.UNPAID">⏳</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.PAID">✅</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.SHIPPED">🚚</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.COMPLETE">🎉</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.CANCELED">❌</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.CLOSED">🚫</text>
    <text v-else-if="orderDetail.status === OrderStatusEnum.SERVICING">🛠️</text>
  </view>

  <view class="status-text">
    {{ OrderStatusLabel[orderDetail.status] }}
  </view>

  <view v-if="orderDetail.statusDesc" class="status-desc">
    {{ orderDetail.statusDesc }}
  </view>

  <view v-if="orderDetail.status === OrderStatusEnum.UNPAID && orderDetail.expireTime" class="countdown">
    剩余支付时间: {{ formatCountdown(orderDetail.expireTime) }}
  </view>
</view>
```

---

## 2️⃣ 操作按钮（✅ 全部用枚举）

```html
<view class="action-buttons">
  <!-- 待付款 -->
  <template v-if="orderDetail.status === OrderStatusEnum.UNPAID">
    <button class="btn secondary" @click="cancelOrder">取消订单</button>
    <button class="btn primary" @click="goToPay">立即支付</button>
  </template>

  <!-- 已付款 / 已发货 -->
  <template v-if="[OrderStatusEnum.PAID, OrderStatusEnum.SHIPPED].includes(orderDetail.status)">
    <button v-if="orderDetail.canRefund" class="btn secondary" @click="applyRefund">
      申请退款
    </button>
    <button
      v-if="orderDetail.status === OrderStatusEnum.SHIPPED"
      class="btn primary"
      @click="confirmReceipt"
    >
      确认收货
    </button>
  </template>

  <!-- 已完成 -->
  <template v-if="orderDetail.status === OrderStatusEnum.COMPLETE">
    <button v-if="!orderDetail.isCommented" class="btn secondary" @click="goToComment">
      评价商品
    </button>
    <button v-if="orderDetail.canRefund" class="btn secondary" @click="applyRefund">
      申请退款/售后
    </button>
    <button class="btn primary" @click="buyAgain">再次购买</button>
  </template>

  <!-- 已取消 / 已关闭 / 售后中 -->
  <template v-if="[OrderStatusEnum.CANCELED, OrderStatusEnum.CLOSED, OrderStatusEnum.SERVICING].includes(orderDetail.status)">
    <button class="btn primary" @click="deleteOrder">删除订单</button>
    <button class="btn primary" @click="buyAgain">重新购买</button>
  </template>
</view>
```

---

## 3️⃣ 底部栏

```html
<view class="bottom-bar">
  <view class="bottom-actions">
    <button
      v-if="orderDetail.status === OrderStatusEnum.UNPAID"
      class="btn bottom-pay"
      @click="goToPay"
    >
      立即支付 ¥{{ orderDetail.totalAmount }}
    </button>
    <view v-else class="contact-section">
      <button class="btn contact" @click="contactCustomer">联系客服</button>
      <button class="btn share" @click="shareOrder">分享订单</button>
    </view>
  </view>
</view>
```

---

# 五、✅ Script 必须这样改（关键）

### ✅ 删除你原来的 `statusTextMap`

```ts
// ❌ 全部删除
const statusTextMap = { ... }
const statusEnumMap = { ... }
```

---

### ✅ 初始化状态

```ts
const orderDetail = ref({
  id: "",
  orderSn: "",
  status: OrderStatusEnum.UNPAID, // ✅ 只用枚举
  statusDesc: "",
  totalAmount: 0,
  ...
});
```

---

### ✅ 赋值（后端直接给 code）

```ts
orderDetail.value = {
  ...
  status: data.status as OrderStatusEnum, // ✅ 后端 0/1/2/3/4/5/6
  ...
};
```

---

### ✅ computed（✅ 正确）

```ts
const showActionButtons = computed(() =>
  [
    OrderStatusEnum.UNPAID,
    OrderStatusEnum.PAID,
    OrderStatusEnum.SHIPPED,
    OrderStatusEnum.COMPLETE,
    OrderStatusEnum.CANCELED,
    OrderStatusEnum.CLOSED,
    OrderStatusEnum.SERVICING,
  ].includes(orderDetail.value.status)
);
```

---

# 六、✅ Style 不用大改（只确认 class 存在）

你现在的 SCSS 已经是 ✅ 正确的：

```scss
&.pending { ... }
&.paid { ... }
&.shipped { ... }
&.completed { ... }
&.cancelled { ... }
&.closed { ... }
&.servicing { ... }
```

✅ 和 `OrderStatusClass` 一一对应

---

# 七、✅ 最终一句话总结（你可以贴 Wiki）

> ✅ 订单状态以后端 `OrderStatusEnum` 为唯一真理源
>
> ❌ 禁止任何魔法字符串（`pending / paid / refunded`）
>
> ❌ 禁止任何魔法数字（`0/1/2`）
>
> ✅ 模板 / JS / CSS / 文案全部收口到枚举
>
> ✅ 新增状态只改枚举，不动页面

---

# 八、下一步我可以继续帮你做的事

1️⃣ 把 支付状态 `PaymentStatusEnum` + 订单状态 `OrderStatusEnum` 合成一张《订单全生命周期状态机图》  
2️⃣ 帮你把 “售后 / 退款 / 退货”拆成独立模块（你现在已经开始碰售后了）  
3️⃣ 帮你把 订单操作权限（谁能点哪个按钮）做成策略表  
4️⃣ 帮你写一份 《前后端状态对齐规范》（给后端 / 前端 / 测试用）

你这套系统，再走完这一步，就已经是“能真收钱、能抗客诉、能撑双 11”的级别了 💰🧱  
要我帮你画那张订单状态机图吗？这张图一旦画对，后面 3 年都不用大改。