你这个 paySuccess 页面已经 90% 正确了，而且用 `Composition API + setup` 是完全对的方向 👍  
我现在直接告诉你：这里怎么用、哪里还能更“生产级”。

---

# 一、先给你一句“终局结论”

> ✅ 这个页面只做三件事：
>
> 1️⃣ 从 URL 取 `orderSn / amount`  
> 2️⃣ 用 `orderSn` 再查一次订单（✅ 强烈建议）  
> 3️⃣ 纯展示 + 跳转  
>
> ❌ 不要再用 URL 传 amount 作为“可信金额”

---

# 二、你当前用得 ✅ 正确的地方

| 项目               | 评价       |
| ------------------ | ---------- |
| Composition API    | ✅ 正确     |
| setup 写法         | ✅ 正确     |
| ref 包裹对象       | ✅ 推荐     |
| URL 参数解析       | ✅ 正确     |
| 跳转订单详情       | ✅ 非常正确 |
| switchTab 返回首页 | ✅ 正确     |

👉 这个页面已经不是 Demo，是“能上线”的页面了

---

# 三、唯一一个“必须改”的点（非常重要）

## ❌ 当前问题

```ts
orderInfo.value.amount = parseFloat(options.amount) || 0;
```

### 问题本质：

> `amount` 是从上一个页面“信任传过来的”
>
> ❌ 前端可篡改  
> ❌ 后端可能已变更  
> ❌ 不符合支付系统原则  

---

## ✅ 正确用法（生产级）

### ✅ 1️⃣ 只用 `orderSn` 查订单

```ts
onLoad(async (options) => {
  console.log("支付成功页面加载，参数:", options);

  const orderSn = options.orderSn;
  if (!orderSn) {
    uni.showToast({ title: "订单号不存在", icon: "none" });
    return;
  }

  try {
    const res = await OrderAPI.getOrderDetail(orderSn);
    orderInfo.value = {
      orderSn: res.orderSn,
      amount: res.paymentAmount / 100, // ✅ 后端金额
    };
  } catch (e) {
    console.error("查询订单失败", e);
    uni.showToast({ title: "订单信息查询失败", icon: "none" });
  }
});
```

✅ 金额以后端为准
✅ URL 参数只做“定位”
✅ 这是支付系统铁律

---

# 四、你这个页面「该怎么用 PaymentStatusEnum」

你现在页面只展示成功，但未来一定会遇到：

• 支付结果未知

• 支付失败

• 已退款


### ✅ 引入枚举（强烈推荐）

```ts
import { PaymentStatusEnum, PaymentStatusLabel } from "@/enums/PaymentStatusEnum";
```

### ✅ 页面数据扩展

```ts
const orderInfo = ref({
  orderSn: "",
  amount: 0,
  paymentStatus: PaymentStatusEnum.PAID,
});
```

### ✅ 展示状态文案

```html
<view class="status-text">
  {{ PaymentStatusLabel[orderInfo.paymentStatus] }}
</view>
```

✅ 前后端状态 100% 对齐
✅ 产品 / 测试都能看懂

---

# 五、你这个页面的「正确职责边界」

| 该做的事 ✅   | 不该做的事 ❌    |
| ------------ | --------------- |
| 展示支付结果 | ❌ 再发起支付    |
| 查订单详情   | ❌ 信任 URL 金额 |
| 跳转订单详情 | ❌ 处理支付回调  |
| 返回首页     | ❌ 修改订单状态  |

👉 这是一个“纯结果展示页”

---

# 六、按钮跳转逻辑（你这里已经是✅ 标准答案）

### ✅ 查看订单详情（正确）

```html
<navigator
  :url="`/packageD/pages/order/detail?orderSn=${orderInfo.orderSn}`"
  open-type="redirect"
  class="action-button"
>
  查看订单详情
</navigator>
```

### ✅ 返回首页（正确）

```html
<navigator
  url="/pages/index/index"
  open-type="switchTab"
  class="action-button hollow"
>
  返回首页
</navigator>
```

✅ redirect 防止返回死循环
✅ switchTab 匹配 tabBar

---

# 七、样式层面的小优化（可选）

### ✅ 给“支付成功”加状态色

```scss
.status-text {
  font-size: 32rpx;
  color: #67c23a; // ✅ 成功绿
  margin-bottom: 40rpx;
}
```

---

# 八、最终一句话总结（你可以拿去复盘）

> ✅ paySuccess 页面只做“展示 + 跳转”
>
> ✅ 金额以后端为准，不信任 URL
>
> ✅ 用 `PaymentStatusEnum` 统一管理状态文案
>
> ❌ 不做任何支付决策
>
> ❌ 不信任前端传值

---

## 九、下一步我可以帮你做的事

1️⃣ 帮你把 `paySuccess` 改成“支持成功 / 失败 / 未知三态”的通用页  
2️⃣ 帮你把 订单详情页的支付状态展示 和这个对齐  
3️⃣ 帮你画一张 《支付结果页职责边界图》（给产品 / 测试看）  
4️⃣ 帮你把 退款、关单、异常单 的结果页一并补齐  

你现在这套，已经是“能真收钱 + 能抗客诉”的级别了 💰  
要我帮你把 paySuccess 升级成“三态通用结果页” 吗？